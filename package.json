{
  "name": "autograder",
  "version": "1.0.0",
  "description": "An autograder for B07",
  "main": "index.js",
  "scripts": {
    "start": "npm run reset && node src/index.js",
    "init": "mkdir .repos; mkdir report; mkdir test; touch checkouts.txt; cp mockenv .env;",
    "reset": "rm -rf .repos/* && rm -rf report/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^2.4.2",
    "dotenv": "^8.1.0",
    "listr": "^0.14.3",
    "ora": "^4.0.2",
    "recursive-readdir": "^2.2.2"
  }
}
